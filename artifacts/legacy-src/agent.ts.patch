// =============================================================================
// agent.ts — TARGETED PATCHES
// Apply each hunk individually. Line numbers are approximate — search for the
// surrounding context strings if they drift after other edits.
// =============================================================================

// =============================================================================
// PATCH 1: ToolRouter constructor call — remove LLM arg, no llmDecision
// =============================================================================
//
// BEFORE (constructor, ~line 52):
//   this.toolRouter = new ToolRouter(this.llm, this.toolSystem);
//
// AFTER:
//   this.toolRouter = new ToolRouter(this.toolSystem);
//
// Rationale: ToolRouter no longer needs the LLM instance. All routing is
// deterministic. Keeping the arg would cause a TypeScript compile error.


// =============================================================================
// PATCH 2: toolRouter.decide() call — add await removal (it's now sync)
// =============================================================================
//
// BEFORE (~line 359):
//   const decision = await this.toolRouter.decide(intent, step.action);
//
// AFTER:
//   const decision = this.toolRouter.decide(intent, step.action);
//
// Also applies to fastMode tryDirectFastTool (~line 213):
// BEFORE:
//   const decision = await this.toolRouter.decide({...}, userInput);
// AFTER:
//   const decision = this.toolRouter.decide({...}, userInput);


// =============================================================================
// PATCH 3: Cache hit — finalize step state before continue
// =============================================================================
//
// BEFORE (~lines 410-423):
//   if (cacheableTools.has(selectedTool)) {
//     const cached = toolCache.get(cacheKey);
//     if (cached && cached.result?.success) {
//       toolResult = cached.result;
//       results.push(toolResult.output);
//       this.emitNarration(`✔ Resultado reutilizado: ${selectedTool}`);
//       this.state.addToolCall({
//         tool: selectedTool,
//         args: args,
//         result: toolResult,
//         timestamp: Date.now()
//       });
//       continue;  // BUG: step never marked done, advanceStep never called
//     }
//   }
//
// AFTER:
//   if (cacheableTools.has(selectedTool)) {
//     const cached = toolCache.get(cacheKey);
//     if (cached && cached.result?.success) {
//       toolResult = cached.result;
//       results.push(toolResult.output);
//       this.emitNarration(`✔ Resultado reutilizado: ${selectedTool}`);
//       this.state.addToolCall({
//         tool: selectedTool,
//         args: args,
//         result: toolResult,
//         timestamp: Date.now()
//       });
//       // FIX: finalize step state before skipping to next iteration
//       this.state.updateStepStatus(step.id, 'done', toolResult.output);
//       stateStore.updateTodoStatus(i, 'done');
//       this.state.advanceStep();
//       continue;
//     }
//   }


// =============================================================================
// PATCH 4: Loop guard — remove destructive read→write promotion
// =============================================================================
//
// BEFORE (~lines 373-393):
//   if (selectedTool === 'read_file') {
//     const pathFromArgs = String(args.path || step.target || this.inferPath(step.action) || '').trim();
//     if (pathFromArgs) {
//       args.path = pathFromArgs;
//     }
//     const readSignature = String(args.path || 'unknown-path');
//     const readCount = (repeatedReadCount.get(readSignature) || 0) + 1;
//     repeatedReadCount.set(readSignature, readCount);
//
//     if (readCount >= 2 && args.path) {
//       try {
//         const fs = await import('fs/promises');
//         await fs.access(String(args.path));
//         selectedTool = 'apply_patch';
//         this.emitNarration(`↺ Loop guard: promovendo read_file para apply_patch em ${args.path}`);
//       } catch {
//         selectedTool = 'write_file';
//         this.emitNarration(`↺ Loop guard: promovendo read_file para write_file em ${args.path}`);
//       }
//     }
//   }
//
// AFTER:
//   if (selectedTool === 'read_file') {
//     const pathFromArgs = String(args.path || step.target || this.inferPath(step.action) || '').trim();
//     if (pathFromArgs) {
//       args.path = pathFromArgs;
//     }
//     const readSignature = String(args.path || 'unknown-path');
//     const readCount = (repeatedReadCount.get(readSignature) || 0) + 1;
//     repeatedReadCount.set(readSignature, readCount);
//
//     // FIX: instead of blindly promoting to a write tool (which executes with
//     // empty oldContent and corrupts the file), skip the redundant read and
//     // reuse the already-cached result. The cache will have the content from
//     // the first successful read.
//     if (readCount >= 2 && args.path) {
//       const cached = toolCache.get(this.buildToolCacheKey('read_file', args));
//       if (cached?.result?.success) {
//         toolResult = cached.result;
//         results.push(toolResult.output);
//         this.emitNarration(`↺ Loop guard: reusing cached read for ${args.path}`);
//         this.state.updateStepStatus(step.id, 'done', toolResult.output);
//         stateStore.updateTodoStatus(i, 'done');
//         this.state.advanceStep();
//         continue;
//       }
//       // No cache (first read failed) — allow retry but warn
//       this.emitNarration(`↺ Loop guard: retrying read for ${args.path} (no cache hit)`);
//     }
//   }


// =============================================================================
// PATCH 5: Remove duplicate addWorkingFile call for write_file
// =============================================================================
//
// BEFORE (~lines 588-595):
//   if ((selectedTool === 'write_file' || selectedTool === 'apply_patch') && args.path) {
//     this.state.addWorkingFile(String(args.path));    // <-- first call (keep this)
//   }
//
//   if (selectedTool === 'write_file' && args.path) {
//     await this.ensureCompanionCheckForNewFile(String(args.path), intent.query);
//     this.state.addWorkingFile(String(args.path));    // <-- BUG: duplicate call
//   }
//
// AFTER:
//   if ((selectedTool === 'write_file' || selectedTool === 'apply_patch') && args.path) {
//     this.state.addWorkingFile(String(args.path));
//   }
//
//   if (selectedTool === 'write_file' && args.path) {
//     await this.ensureCompanionCheckForNewFile(String(args.path), intent.query);
//     // FIX: removed duplicate addWorkingFile call — already added above
//   }


// =============================================================================
// PATCH 6: reflector.reflect() — pass toolName as 4th argument
// =============================================================================
//
// BEFORE (~line 786):
//   const reflection = await this.reflector.reflect(step.action, toolResult, intent.query);
//
// AFTER:
//   const reflection = await this.reflector.reflect(step.action, toolResult, intent.query, selectedTool);
//
// Rationale: reflector.ts now uses toolName for structural decisions instead
// of parsing Portuguese strings in tool output. Without this change, all
// file writes will still go through the expensive deepReflect LLM call.


// =============================================================================
// PATCH 7: buildMessages — fix deduplication logic
// =============================================================================
//
// BEFORE (~line 928):
//   if (!recentMessages.some(m => m.content === currentQuery)) {
//     messages.push({ role: 'user', content: currentQuery });
//   }
//
// AFTER:
//   // FIX: compare against the last user message only, not all messages.
//   // The old check compared step.action against full message history content,
//   // which could silently drop a step action that happened to match an earlier
//   // assistant response string.
//   const lastUserMsg = [...recentMessages].reverse().find(m => m.role === 'user');
//   if (!lastUserMsg || lastUserMsg.content !== currentQuery) {
//     messages.push({ role: 'user', content: currentQuery });
//   }


// =============================================================================
// PATCH 8: Remove dead path inspection blocks
// =============================================================================
//
// The blocks at ~lines 539-575 (the second inspectPath + ensureReadBeforeEdit
// and the third fs.readFile attempt) are dead code because pathInspected=true
// is set in the first block (~line 477) before these are reached.
//
// REMOVE these two blocks entirely:
//
//   // Block A (~lines 539-559) — REMOVE:
//   if ((selectedTool === 'write_file' || selectedTool === 'apply_patch') && args.path && !pathInspected) {
//     try { ... } catch { ... }
//   }
//
//   // Block B (~lines 561-570) — REMOVE:
//   if ((selectedTool === 'write_file' || selectedTool === 'apply_patch') && args.path && fileExisted && !oldContentLoaded) {
//     try {
//       const fs = await import('fs/promises');
//       oldContent = await fs.readFile(args.path, 'utf-8');
//       ...
//     } catch { ... }
//   }
//
// Rationale: lines 469-489 already handle inspection + read. These blocks
// execute only if the first inspection threw an exception, in which case the
// code already did `continue` at line 479. They are unreachable in practice.
